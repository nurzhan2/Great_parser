# --- Core (нужно всегда, ставится быстро, без GPU) ---
requests>=2.31
Pillow>=10.0
XlsxWriter>=3.1
numpy>=1.24
PyYAML>=6.0

# --- Опционально: детекция / OCR / классификация (GPU) ---
# Ставить по мере подключения соответствующих этапов пайплайна.
# ultralytics>=8.2         # YOLO-детектор баннеров
# easyocr>=1.7             # OCR (рус+англ)
# open-clip-torch>=2.24    # zero-shot классификация темы
# torch                    # бэкенд для YOLO/CLIP (ставить под свою CUDA)

# --- Опционально: seed-точки по дорожному графу ---
# osmnx>=1.9               # дорожный граф из OpenStreetMap
